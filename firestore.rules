rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary rule for debugging - ALLOW ALL authenticated users
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}