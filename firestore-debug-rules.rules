rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary rule for debugging - ALLOW ALL authenticated users
    // This is NOT secure but will help us debug the permission issue
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
